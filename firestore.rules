rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ── NOTICES ─────────────────────────────────────────────────────────────
    // Public: anyone can read notices (home page notice board)
    // Admin:  only signed-in users can create/update/delete
    match /notices/{noticeId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // ── COMPLAINTS ───────────────────────────────────────────────────────────
    // Public: anyone can create a complaint (complaint form on home page)
    // Public: anyone can read complaints (for tracking by ID + mobile)
    // Admin:  only signed-in users can update/delete
    match /complaints/{complaintId} {
      allow read, create: if true;
      allow update, delete: if request.auth != null;
    }

    // ── NEWS ─────────────────────────────────────────────────────────────────
    // Public: anyone can read news (home page latest news)
    // Admin:  only signed-in users can write
    match /news/{newsId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // ── PROJECTS ─────────────────────────────────────────────────────────────
    // Public: anyone can read projects (home page projects section)
    // Admin:  only signed-in users can write
    match /projects/{projectId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // ── ROUTINE ──────────────────────────────────────────────────────────────
    match /routine/{id} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // ── OFFICIALS ────────────────────────────────────────────────────────────
    match /officials/{id} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // ── GALLERY ──────────────────────────────────────────────────────────────
    match /gallery/{id} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // ── SABHA ─────────────────────────────────────────────────────────────────
    match /sabha/{id} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // ── SCHEMES ──────────────────────────────────────────────────────────────
    match /schemes/{id} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // ── DENY EVERYTHING ELSE ─────────────────────────────────────────────────
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
